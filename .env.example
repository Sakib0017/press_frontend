# Frontend env — set VITE_API_URL to your deployed backend URL (no trailing slash)
# For local dev use http://localhost:5000 or leave empty to use Vite proxy (/api -> :5000)
VITE_API_URL=http://localhost:5000

# Production example (your live backend):
# VITE_API_URL=https://press-backend-alpha.vercel.app
