# Frontend env — set VITE_API_URL to your deployed backend URL (no trailing slash)
# For local dev you can leave it empty — it will use /api via Vite proxy
VITE_API_URL=https://your-backend.vercel.app

# Example:
# VITE_API_URL=https://press-mern-backend.vercel.app
